specVersion: "v1"
serviceId: "maintenance-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "maintenance-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
